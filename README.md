# PRISM

An interactive tool for exploring how AI vision models respond when an image is changed.

_Full README (what it does, how it works, architecture, running locally, limitations) will be written once the MVP is working — see project steps in progress._
