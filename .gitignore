# Python
backend/.venv/
backend/venv/
__pycache__/
*.pyc

# Node / frontend
frontend/node_modules/
frontend/dist/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Env
.env
.env.local

# Model cache (Hugging Face downloads these locally, don't commit them)
backend/.cache/
huggingface_cache/

# Temp uploaded images
backend/app/tmp_uploads/

# Research script source images — sampled from Oxford-IIIT Pet at run time,
# not app code. manifest.csv (which image, which label) stays tracked for
# reproducibility/attribution; the actual jpgs don't.
backend/research/data/oxford_pets/

# Local tooling state
.claude/
